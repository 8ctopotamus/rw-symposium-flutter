import 'package:flutter/material.dart';

class PresentationsScreen extends StatelessWidget {
  static const String id = '';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Presentations 2020'),
      ),
    );
  }
}
